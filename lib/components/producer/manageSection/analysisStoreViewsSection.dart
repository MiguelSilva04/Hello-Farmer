import 'package:flutter/material.dart';

class AnalysisStoreViewsSection extends StatelessWidget {
  const AnalysisStoreViewsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("A.StoreViews"));
  }
}
