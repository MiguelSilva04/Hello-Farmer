import 'package:flutter/material.dart';

class AddsSection extends StatelessWidget {
  const AddsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Adds"));
  }
}
