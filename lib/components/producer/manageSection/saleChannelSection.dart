import 'package:flutter/material.dart';

class SaleChannelSection extends StatelessWidget {
  const SaleChannelSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("SaleChannels"));
  }
}
