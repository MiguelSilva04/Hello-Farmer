import 'package:flutter/material.dart';

class ManagePricesSection extends StatelessWidget {
  const ManagePricesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("ManagePrices"));
  }
}
