import 'package:flutter/material.dart';

class HighlightedAddsSection extends StatelessWidget {
  const HighlightedAddsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("HighlightedAdds"));
  }
}
