import 'package:flutter/material.dart';

class ManageStockSection extends StatelessWidget {
  const ManageStockSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("ManageStock"));
  }
}
