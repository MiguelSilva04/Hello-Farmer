import 'package:flutter/material.dart';

class AnalysisReportsSection extends StatelessWidget {
  const AnalysisReportsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("A.Reports"));
  }
}
