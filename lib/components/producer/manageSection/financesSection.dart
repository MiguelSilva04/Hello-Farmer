import 'package:flutter/material.dart';

class FinancesSection extends StatelessWidget {
  const FinancesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Finances"));
  }
}
